export type ExploreItemType = {
  icon: string;
  alt: string;
  title: string;
  description: string;
  align?: "left" | "right";
};

export type MenuItemType = {
  imageSrc: string;
  name: string;
  description: string;
  price: number;
};
